# v0.1.0 스토어 스크린샷 (이전 기록)

`eolma-exporter` **v0.1.0** 시점의 Chrome 웹 스토어 등록용 스크린샷이다.
이후 UI가 바뀌어 더 이상 현재 버전을 반영하지 않으므로 이력 보존용으로 이 폴더에 남긴다.

| 파일 | 설명 |
|---|---|
| `store-ko-naverpay.png` | 네이버페이 주문내역 페이지에서의 팝업 (한국어) |
| `store-ko-naverpay-browser.png` | 브라우저 화면 맥락 포함 (한국어) |

현재 버전 스크린샷은 상위 `screenshots/` 폴더를 참조.
